# Questions - 2026

No open user-action blockers as of 2026-05-07.

Historical note: GitHub workflow-file pushes may require a local GitHub CLI token with `workflow` scope. If a future push is rejected while editing `.github/workflows/*.yml`, run:

```bash
gh auth refresh -h github.com -s workflow
```
